<%= form_with(model: report) do |form| %>
  <% if report.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(report.errors.count, "error") %> prohibited this report from being saved:</h2>

      <ul>
        <% report.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :report_title, style: "display: block" %>
    <%= form.text_field :report_title %>
  </div>

  <div>
    <%= form.label :report_body, style: "display: block" %>
    <%= form.text_area :report_body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
