<p style="color: green"><%= notice %></p>

<h1><%= t 'views.books.index.title' %></h1>

<div id="books">
  <% @books.each do |book| %>
    <%= render book %>
    <p>
      <%= link_to show_text, book %>
    </p>
  <% end %>
</div>

<%= link_to new_text, new_book_path %>
